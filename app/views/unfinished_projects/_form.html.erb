<%= form_for(@unfinished_project) do |f| %>
  <% if @unfinished_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unfinished_project.errors.count, "error") %> prohibited this unfinished_project from being saved:</h2>

      <ul>
      <% @unfinished_project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :repo_url %><br />
    <%= f.text_field :repo_url %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br />
    <%= f.text_field :image_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
